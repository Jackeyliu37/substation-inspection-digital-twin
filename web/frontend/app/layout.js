import "./globals.css";

export const metadata = { title: "Substation Inspection", description: "Phase 1 environment baseline" };

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
