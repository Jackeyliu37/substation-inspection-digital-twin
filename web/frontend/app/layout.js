import "./globals.css";

export const metadata = { title: "变电站智能巡检控制中心", description: "Gateway-operated substation inspection control center" };

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
